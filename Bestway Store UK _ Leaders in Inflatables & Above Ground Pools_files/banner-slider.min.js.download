require(["jquery"],function($){var $screensize=null;var $current=1;function banners(){$(window).resize(function(){$screensize=$('body').width();if($screensize>1024){$('.header-confidence-banner').find('ul').removeClass('banners');$('.header-confidence-banner').find('li').addClass('displayed');}
if($screensize<=1023){$('.header-confidence-banner').find('ul').addClass('banners');}});$('.banners').find('.displayed').toggleClass('displayed');$('.banners').find('.banner').filter(':nth-child('+$current+')').addClass('displayed');$current=$current+1;if($current==4){$current=1;}}
banners();setInterval(banners,4000);});